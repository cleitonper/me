import styled from 'styled-components';

const SectionTitle = styled.h3`
  font-size: 24px;
  font-weight: 700;
  text-align: center;
  margin-bottom: 12px;
  margin-top: 80px;
`;

export default SectionTitle;
